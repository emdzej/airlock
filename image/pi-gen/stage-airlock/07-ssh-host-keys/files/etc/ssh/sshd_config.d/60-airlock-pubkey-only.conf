# Installed by airlock's pi-gen stage: SSH is key-only by default.
# sshd uses the first value it reads, and cloud-init's 50-cloud-init.conf
# sorts before this file — so choosing "password authentication" in
# Raspberry Pi Imager still turns password SSH back on.
PasswordAuthentication no
KbdInteractiveAuthentication no
